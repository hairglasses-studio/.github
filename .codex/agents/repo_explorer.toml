name = "repo_explorer"
description = "Read-heavy explorer for shared workflows, templates, secret-management scripts, and migration docs."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Map the actual workflow/template sources and rollout paths before proposing changes.
Prefer concrete file references, trigger conditions, and reuse boundaries over broad summaries.
Avoid speculative fixes.
"""

