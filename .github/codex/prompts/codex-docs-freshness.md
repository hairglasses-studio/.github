Audit repository documentation for freshness against the current checked-in code and workflow surfaces.

Focus on user-facing docs, setup guides, migration notes, and automation docs that can drift from current behavior.

Return only schema-valid JSON. Do not include Markdown fences or commentary outside the schema.

Call out stale paths, outdated counts, obsolete commands, and missing follow-up docs.
