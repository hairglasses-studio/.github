Extract a concise, source-backed MCP inventory for the repository.

Focus on repo-local `.mcp.json`, Codex MCP config blocks, install/register commands, and any documented MCP contract surfaces.

Return only schema-valid JSON. Do not include Markdown fences or commentary outside the schema.

Prefer explicit server names, config paths, transport details, and drift gaps over speculative interpretation.
