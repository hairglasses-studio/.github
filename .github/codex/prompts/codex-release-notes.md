Generate release notes from the requested git range.

Focus on user-visible changes, operational changes, upgrade considerations, and documentation updates that should be called out in a release summary.

Return only schema-valid JSON. Do not include Markdown fences or commentary outside the schema.

Do not invent changes that are not supported by the diff or checked-in docs.
