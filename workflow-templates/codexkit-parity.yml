name: Codexkit Parity

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  codexkit-parity:
    uses: hairglasses-studio/.github/.github/workflows/codexkit-parity.yml@main
    with:
      repo-name: ${{ github.event.repository.name }}
